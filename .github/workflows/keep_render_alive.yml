on:
  schedule:
    # Rodar a cada 5 ou 10 minutos para compensar o atraso do GitHub
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: 🔄 Ping Render
        run: |
          # O comando "|| true" no final impede que o GitHub marque como Erro se o ping falhar
          curl -s -f https://atlas-backend-fn5s.onrender.com/api/health || echo "Servidor ainda acordando..."
          
          # Ping na URL principal também
          curl -s -f https://atlas-backend-fn5s.onrender.com || echo "Frontend ainda acordando..."

          